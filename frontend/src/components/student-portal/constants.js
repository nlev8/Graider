// Moved verbatim from StudentPortal.jsx (CQ wave-6 split). Shared by the
// shell's preloaded-accommodation useEffect and createPortalHandlers'
// handleStartAssessment — the same single list the inline `var` was before.

// Delivery accommodation preset IDs for conditional checks
export var DELIVERY_PRESET_IDS = ["extended_time_1_5x", "extended_time_2x", "extended_time_unlimited",
  "large_text", "read_aloud", "reduced_distractions"];
