[pytest]
pythonpath = .
testpaths = tests
markers =
    live: Live API tests requiring OPENAI_API_KEY (deselect with: -m "not live")
    stress: Stress tests with high concurrency / rapid polling (deselect with: -m "not stress")
