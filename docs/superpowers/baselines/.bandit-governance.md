# Bandit Baseline Governance

## Overview

The `.bandit-baseline.json` file is the security baseline for Phase 5a (PR A). It documents known acceptable security findings (or findings deferred for future hardening phases).

## Review Cadence

This baseline should be reviewed and refreshed:
- At the start of every new phase plan
- Anytime a flagged pattern is either fixed (remove from allow-list) or newly introduced during a phase

## Refresh Command

```bash
bandit -r backend/ --exclude backend/scripts,backend/migrations -ll -iii -f json -o .bandit-baseline.json
```

**Note on format:** Bandit's `--baseline` flag requires JSON input — it does NOT parse YAML baselines (Bandit's baseline loader uses `json.loads`). This is why the file is `.json` and not `.yaml`, and why governance documentation lives in this sibling `.md` file rather than inline.

## Flags Explained

- `-ll`: Show MEDIUM and above severity (Bandit's `-l` flag is cumulative "show LOW+"; `-ll` means "show MEDIUM+")
- `-iii`: Requires HIGH confidence
- **Effective filter**: MEDIUM+ severity AND HIGH confidence only

## Current Known Issues

As of 2026-04-21, one HIGH severity / HIGH confidence issue is flagged:
- **B324** (hashlib): Use of weak MD5 hash for security in `backend/services/assistant_tools_edtech.py:115`
  - **Context**: Used for topic+assignment deduplication, not cryptographic; consider `usedforsecurity=False`
  - **Phase for Fix**: Phase 6 (Excellence hardening)

## Future Actions

1. Monitor for new HIGH+HIGH findings (should trigger immediate fix in same phase)
2. Update baseline after each phase completion or when patterns are fixed
