# Graider SEO/GEO Monitor — 2026-06-08

Recurring AI-search-position watch (see [`docs/seo/PLAN.md`](../PLAN.md), Tier 0.2).
Goal: entity primacy for **Graider / graider.live** vs **grAIder.de** (German competitor).

## 1. On-page audit

**`python3 scripts/seo_audit.py landing` → PASSED** — all on-page invariants intact
(JSON-LD parses; required @types WebSite/SoftwareApplication/Organization/FAQPage present;
`Organization.sameAs` non-empty; AI crawlers allowed in robots.txt; sitemap directive +
`llms.txt`/`sitemap.xml` present; title/description/canonical present; no dead `href="#"`
links — the three login `href="#"` are JS `onclick` handlers, correctly excluded).

## 2. SERP / AI-answer position (US, WebSearch)

| Query | graider.live position | grAIder.de present? | AI-answer conflation |
|---|---|---|---|
| **"Graider"** | **#1** (top result; AI summary leads with our product) | No (US localization holds) | **Yes** — AI answer blends us with `graider.vercel.app` (a different code-grader), *The Graide Network*, and SoundCloud/Ancestry noise |
| **"Graider AI grading"** | **#1** + our `/blog/graider-vs-*` comparison posts rank | Yes, ~#8 (`grAIder · AI-assisted grading software`) | Low — AI answer is **all graider.live**, accurate & detailed |
| **"what is Graider"** | **#1**; AI answer all graider.live | No | Low — clean, accurate |
| **"best AI grading tool for teachers"** | **Absent** (top 10 = Gradescope, CoGrader, EssayGrader, Turnitin, MagicSchool, GradeWithAI, Graded Pro, StarGrader) | n/a | n/a — high-competition generic; we don't rank |
| **"AI grading Clever ClassLink OneRoster"** | **Absent** (top = Frizzle, Clever, GreatMinds, GoGuardian) | No | n/a — the uncontested SIS query we *should* own (PLAN 2.4) but don't yet |

**Brand-term verdict:** we **own "Graider" (#1)** in US Google for the three brand/intent queries, and grAIder.de does **not** surface on "Graider" / "what is Graider" — Google's US localization is working as the plan predicted. Residual exposure is (a) AI answers conflating multiple same-name "grAIder" entities (not specifically .de), and (b) **zero presence on the SIS-integration query**, which is our strategic moat.

## 3. grAIder.de competitor snapshot (WebFetch)

- **Positioning:** *"Weniger Korrektur. Mehr Zeit für das, was zählt."* — efficiency/transparency, "developed by teachers."
- **Market:** German-speaking EU; **claims 500+ schools across all 16 German states**. German-primary (EN/ES available).
- **Features:** upload → AI grading drafts → customize/release; criterion-based grading; handwritten support.
- **Pricing:** free registration/trial; no paid tiers shown.
- **Integrations:** **none disclosed** — no Clever/ClassLink/OneRoster/LMS.
- **New since last watch:** no new awards/launches/pricing on the page.
- **Read:** They remain **EU-only with no SIS integrations** — our US K-12 + Clever/ClassLink/OneRoster + IEP/504 positioning is **uncontested by them**. Their one notable authority signal is the "500+ schools" claim (off-page social proof we should match with US reviews/launch).

## 4. On-page fix shipped this run

**Gap found:** `landing/index.html` advertises SIS integration **16×** (Clever-certified SSO +
roster sync, ClassLink/OneRoster, JSON-LD feature list, login buttons), but `landing/llms.txt`
— the file AI crawlers (GPTBot/ClaudeBot/PerplexityBot/Google-Extended) read for entity
grounding — had **zero** Clever/ClassLink/OneRoster mentions. That inconsistency is a direct
cause of finding #5 (absent on the SIS query): the AI-grounding file never told AI engines we
do SIS rostering.

**Fix:** added a `SIS & Rostering Integration (Clever, ClassLink, OneRoster, LTI 1.3)` feature
bullet + a districts-on-Clever/ClassLink/OneRoster audience line to `landing/llms.txt`
(mirroring the existing, accurate index.html claims). Audit re-run: **PASSED**. This is the
cheap GEO win; the full SIS landing **pages** (PLAN 2.4) remain the larger follow-up.

## 5. Recommended actions (ranked)

1. **Build the SIS-integration landing pages (PLAN 2.4)** — highest-leverage gap. We're absent
   for "AI grading Clever/ClassLink/OneRoster", an uncontested query grAIder.de can't target.
   The llms.txt fix above is the down-payment; dedicated pages + a "Graider for Clever/ClassLink/
   OneRoster districts" hub are the real win. *(Owner: in-repo/me.)*
2. **Anchor the "Graider" entity to kill AI-answer conflation (PLAN 1.1–1.3)** — create the
   **Wikidata item** + claim X/LinkedIn/GitHub/Crunchbase/Product Hunt and feed live handles into
   JSON-LD `sameAs`. AI answers currently blend us with `graider.vercel.app` / The Graide Network;
   Wikidata + sameAs is what disambiguates. *(Owner: you off-platform; I finalize sameAs.)*
3. **Ship the "What is Graider?" entity page (PLAN 3.3)** — reinforce the #1 we already hold and
   hand AI engines a canonical definition. *(Owner: me.)*
4. **Match grAIder.de's social proof (PLAN 2.1–2.2)** — Product Hunt launch + seed G2/Capterra
   reviews for US-context corroboration AI engines triangulate. Counters their "500+ schools".
   *(Owner: you.)*
5. **Long-game on the generic query** — "best AI grading tool for teachers" is dominated by
   incumbents; our `graider-vs-*` comparison posts are the wedge. Keep expanding them; don't
   chase the generic head term directly yet. *(Owner: me, ongoing.)*

---
*Audit: PASSED. Brand term "Graider": #1 (US). PR'd: `landing/llms.txt` SIS-integration
disambiguation fix (Class A, left for human review).*
